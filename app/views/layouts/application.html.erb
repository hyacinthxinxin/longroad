<!DOCTYPE html>
<html>
  <head>
    <title>CS1119IT | <%= yield(:title) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="page-wrap">
      <div class='container'>
        <%= render 'shared/messages' %>
        <%= yield %>
        <%= debug(params) if Rails.env.development? %>
      </div>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
