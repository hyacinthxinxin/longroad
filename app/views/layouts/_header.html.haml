%body
  %header.navbar.navbar-fixed-top.navbar-inverse
    .container
      = link_to "乐控", root_path, id: "logo"
      %nav
        %ul.nav.navbar-nav.navbar-right
          - if current_user
            %li.dropdown
              %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
                欢迎 #{current_user.email}
                %b.caret
              %ul.dropdown-menu
                %li= link_to "我的项目", buildings_path
                %li.divider
                %li= link_to '修改密码', edit_registration_path(:user)
                %li.divider
                %li= link_to '退出登录', destroy_user_session_path, :method => :delete
          - else
            %li= link_to('首页', root_path)
            %li= link_to('登录', new_session_path(:user))
            %li= link_to('注册', new_registration_path(:user))
          %li= link_to('价格', '#')
          %li= link_to('文档', '/help')
          %li= link_to('工具', '#')

